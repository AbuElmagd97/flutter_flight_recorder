/// Records a rolling timeline of app events (errors, network, navigation,
/// actions, logs) so QA and developers get the complete story of what
/// happened before a bug.
library;

export 'src/recorder/flight_recorder.dart';
